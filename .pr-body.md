## Summary

- Add `[data-layer]` / `[ui-layer]` / `[e2e]` AC layer tag enforcement to `otta-gate.sh` via a new `check-ac-layers.sh` sub-check: `[ui-layer]`/`[e2e]` ACs with only unit-test evidence fail the gate; `[data-layer]` ACs pass with unit tests
- Add AC3 warning when unclosed `[ui-layer]`/`[e2e]` ACs remain while only `[data-layer]` ACs are closed (exit 0 — warning only)
- Update `seed-pr-body.sh` to preserve layer tags and inject a layer key note when layer-tagged ACs are detected
- Create `skills/otta-dev.md` and add `--help` to `otta-gate.sh` documenting the layer key for builders and reviewers

```acceptance
GIVEN otta-gate.sh and seed-pr-body.sh support AC layer tags
WHEN a PR body includes checked ACs with [ui-layer], [e2e], or [data-layer] tags
THEN the gate enforces correct evidence type per layer, and seed-pr-body.sh includes the layer key

- [x] AC1 `[data-layer]`: Given `otta-gate.sh` runs on a PR body where a `[ui-layer]` or `[e2e]` AC is checked with evidence that contains only `test`, `unit`, or `npm run` keywords, then the gate fails with message: "AC tagged [ui-layer]/[e2e] requires preview URL or e2e evidence — unit test insufficient." — bash tests/otta-gate-ac-layers.test.sh (test 1 and 2)
- [x] AC2 `[data-layer]`: Given `otta-gate.sh` runs on a PR body where a `[data-layer]` AC is checked with any test evidence, then the gate passes. — bash tests/otta-gate-ac-layers.test.sh (test 3)
- [x] AC3 `[data-layer]`: Given `otta-gate.sh` runs and an issue has unclosed `[ui-layer]` ACs while PR only closes `[data-layer]` ones, then gate warns "Issue has unclosed [ui-layer]/[e2e] ACs — issue will remain open after merge." — bash tests/otta-gate-ac-layers.test.sh (test 4)
- [x] AC4 `[data-layer]`: Given `seed-pr-body.sh` runs for an issue with layer-tagged ACs, then the seeded `.pr-body.md` includes the layer tags and a note on required evidence type per layer. — bash tests/otta-gate-ac-layers.test.sh (test 6)
- [x] AC5 `[data-layer]`: Given the layer key definition, when added to `scripts/otta-gate.sh` help output and `skills/otta-dev.md`, then it is documented for builders and reviewers. — bash tests/otta-gate-ac-layers.test.sh (test 7); skills/otta-dev.md created

## Out of scope
- Changes to CI gate configuration (Pulse merge gates) — local gate only
- Seeding from issues that have no layer tags (unchanged behavior)

## Verification
- unit: `bash tests/otta-gate-ac-layers.test.sh` — 7 tests: AC1×2, AC2, AC3, unchecked-skip, AC4, AC5 — all pass

```

idea_ref: issue:#65

Fixes #65
