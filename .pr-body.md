## Summary

- Adds `learn:` block to `.otta.yml` (enabled, 180-day expiry, weekly cadence) so `ops/run-learn-weekly.sh` picks up this repo automatically once otta-engine #87 merges.

```acceptance
GIVEN otta-build/plugin has gate history in ~/.otta/ledger/otta-build-plugin.jsonl
WHEN  ops/run-learn-weekly.sh runs (otta-engine #87)
THEN  learn-compile and learn-mine-sessions run against this repo's ledger and sessions
```

- [x] AC1: .otta.yml has `learn.enabled: true` so the runner's per-repo gate passes

## Out of scope
- No executable code changed; no new learn rules yet.

## Verification
- unit: n/a — config opt-in only
- e2e: n/a — live runner covered by otta-engine test suite (#87)
- preview: n/a

[test-impractical: config opt-in — no executable code changed]

## Acceptance Evidence

idea_ref: issue:#118

Fixes #118
