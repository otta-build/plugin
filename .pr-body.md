## Summary

- New setup step 9c in `commands/setup.md`: opt-in release versioning (3-option AskUserQuestion — auto-tag, manual, skip)
- New script `scripts/otta-release-setup.sh`: installs `.github/workflows/otta-release.yml` (auto-tag on version bump to main), idempotent, `--dry-run` flag
- New execution step B7 in Part B: runs release script if user chose "Yes"
- Fully opt-in — repos that skip are unaffected

```acceptance
GIVEN a developer runs /otta:setup in a repo with package.json and chooses "Yes — auto-tag on version bump" in step 9c
WHEN setup completes (Part B runs B7)
THEN .github/workflows/otta-release.yml exists and .otta.yml has release.auto: tag_on_version_bump

GIVEN a developer chooses "Skip — no versioning needed" in step 9c
WHEN setup completes
THEN no workflow file is written, .otta.yml has no release block

GIVEN otta-release-setup.sh is run twice on the same repo
WHEN the workflow file already exists
THEN script exits 0 without overwriting (idempotent)

- [x] AC1: commands/setup.md has step 9c with AskUserQuestion (3 options: auto-tag / manual / skip)
- [x] AC2: scripts/otta-release-setup.sh creates .github/workflows/otta-release.yml idempotently
- [x] AC3: --dry-run flag prints what would be written, exits 0, writes nothing
- [x] AC4: tests/release-setup.test.sh covers create + idempotency + dry-run
- [x] AC5: existing tests pass (zero regressions)

## Out of scope
- Multi-language version file detection (Cargo.toml, pyproject.toml)
- otta/version-bump Pulse gate (pulse#56)

## Verification
- unit: `bash tests/release-setup.test.sh` (new) + `bash tests/setup-wizard.test.sh`
- e2e: n/a
- preview: n/a
```

## Acceptance

idea_ref: issue:#55

Fixes #55
