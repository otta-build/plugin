## Summary

- `scripts/write-otta-contract.sh` now emits `version: "1"` at top (otta-engine `resolve_config` requires it)
- Add `--learn` flag → emits active `learn: {enabled: true, expiry_days: 180, cadence: weekly}` block; without it a commented-out `learn:` block is emitted as a reminder
- Add `--learn-expiry-days <N>` and `--learn-cadence <daily|weekly>` to customize learn config
- Emit commented `# models:` block with example model pins (advanced config, not prompted)
- Wizard step 4 "LEARN layer" added to `commands/setup.md` — explains GEPA benefit, asks opt-in, wires to new `--learn` flags in Part B
- `tests/write-otta-contract.test.sh` — 12 tests covering version header, deploy.auto/allow_production, learn disabled/enabled/customized, models comment, and engine YAML structural assertion

```acceptance
GIVEN write-otta-contract.sh is run without any flags
WHEN the contract is written
THEN version: "1" appears before tracker:, and a commented learn: block is emitted

GIVEN write-otta-contract.sh is run with --learn
WHEN the contract is written
THEN an active learn: block with enabled: true, expiry_days: 180, cadence: weekly is emitted

GIVEN the generated .otta.yml is parsed for otta-engine structural checks
WHEN version: "1" is present and all 6 required keys exist
THEN the validation passes (no missing-version error)

GIVEN the generated .otta.yml includes a --learn output
WHEN parsed with PyYAML (or line-scan fallback)
THEN learn.enabled is True and all expected keys are present
```

- [x] AC1: `version: "1"` emitted before `tracker:` in all write-otta-contract.sh output
- [x] AC2: `--learn` flag adds active `learn:` block; absent by default (commented out) — wizard step 4 asks opt-in
- [x] AC3: `# models:` comment block emitted with example pins (`builder`/`reviewer`)
- [x] AC4: `tests/write-otta-contract.test.sh` test 12 validates generated YAML against engine structural rules (version "1" + all 6 schema keys + learn block)
- [x] AC5: `tests/write-otta-contract.test.sh` added with 12 test cases (covers both #101 and #103 AC)

idea_ref: issue:#103

Fixes #103
