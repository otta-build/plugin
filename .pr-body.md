## Summary

- Added `hooks/hooks.claude.json` — the same two hooks (`pre-push-guard` PreToolUse(Bash), `subagent-gate-guard` SubagentStop) in Claude Code's exec `args` array form (`"command": "bash", "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/<script>.sh"]`), confirmed against the live Claude Code hooks docs to support `${CLAUDE_PLUGIN_ROOT}` substitution inside `args` entries with no shell involved.
- `.claude-plugin/plugin.json` now declares `"hooks": "./hooks/hooks.claude.json"` explicitly (previously relied on the `./hooks/hooks.json` default-discovery convention it shared with Codex); `.codex-plugin/plugin.json` and `hooks/hooks.json` (shell-string `bash -c` form) are untouched.
- Added `tests/hooks-harness-split.test.sh`: asserts each manifest points at its own hooks file, `hooks.claude.json` is exec-args-form only (no shell-string commands, every entry has `args`), no `${user_config.*}` leaks in, and both hooks files declare the same events and target the same scripts so they can't drift silently.

```acceptance
GIVEN the plugin is installed as a Claude Code plugin
WHEN  a Bash tool call or SubagentStop event fires
THEN  Claude Code spawns pre-push-guard.sh / subagent-gate-guard.sh directly via exec args form (no shell, ${CLAUDE_PLUGIN_ROOT} resolved) — identical guard behavior to the previous shell-string form, while Codex keeps consuming hooks/hooks.json unchanged
```

- [x] Hook declaration split or transform layer so Claude Code consumes exec `args` form while Codex keeps a functioning shell-string path; behavior identical on both harnesses.
- [x] tests/codex-plugin-parity.test.sh still passes; new test asserts the Claude Code manifest has no shell-string `command` entries.
- [x] No `${user_config.*}` in any shell-form command (regression guard test).

## Out of scope
- Changing the hook SCRIPTS themselves (pre-push-guard.sh, subagent-gate-guard.sh) — unchanged.
- Adding `${user_config.*}` usage to the new exec-form file (not needed by either hook today) — the docs confirm exec form would permit it, but that's not exercised here.

## Verification
- unit: `bash tests/hooks-harness-split.test.sh` (new — 7 checks), `bash tests/codex-plugin-parity.test.sh` (unmodified, 100 checks, still green), `bash tests/hooks-shell-injection-guard.test.sh` (unmodified, still green)
- e2e: n/a — plugin hook loading is a Claude Code/Codex runtime behavior, not locally drivable; covered by manifest/schema assertions per existing test conventions
- preview: n/a — no preview env for a plugin repo

## Acceptance Evidence

- [x] AC1: harness split, identical behavior — evidence: `.claude-plugin/plugin.json` → `./hooks/hooks.claude.json` (exec args form), `.codex-plugin/plugin.json` → `./hooks/hooks.json` (unchanged shell form); both declare the same two hook events targeting the same two scripts (tests/hooks-harness-split.test.sh semantic-equivalence checks).
- [x] AC2: parity + new no-shell-string test — evidence: `tests/codex-plugin-parity.test.sh` still green (100/100), `tests/hooks-harness-split.test.sh` new checks "hooks.claude.json has no shell-string command values" and "entries all use exec args-array form".
- [x] AC3: no `${user_config.*}` regression guard — evidence: `tests/hooks-shell-injection-guard.test.sh` (existing, hooks.json) plus new check in `tests/hooks-harness-split.test.sh` for hooks.claude.json.

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#155

Fixes #155
