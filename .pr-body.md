## Summary

- `/otta:status` with no argument now enumerates open issues (capped at 20) and renders a dashboard instead of erroring.
- Added a compact one-row-per-issue table format (issue #, title, one glyph per stage) to `scripts/otta-status.sh`, reusing the existing single-issue stage-resolution logic documented in `commands/status.md`.
- Existing single-issue mode is unchanged.

```acceptance
GIVEN a repo with open GitHub issues at varying pipeline stages
WHEN  a developer runs `/otta:status` with no issue number
THEN  a scannable dashboard table renders, one row per issue, with a pass/fail/pending glyph per stage (Idea/Build/Gate/CI/Release)

- [x] `commands/status.md` updated: when `$1` is empty/absent, switch to dashboard mode instead of erroring or requiring an issue number.
- [x] Dashboard mode enumerates open issues via `gh issue list` (reasonable default limit, e.g. 20, to avoid unbounded API calls — document the cap), resolves each one's stage the same way single-issue mode does (reuse existing logic, don't duplicate it).
- [x] Renders one compact row per issue (not the full 5-line checklist per issue) — e.g. issue #, title, and a condensed stage indicator (one glyph per stage) — so N issues stay scannable in one screen.
- [x] `scripts/otta-status.sh` extended (or a sibling render path added) to support this list/table format, unit-tested per this repo's `tests/*.test.sh` convention.
- [x] Existing single-issue mode (`/otta:status <issue>`) behavior is unchanged — this is additive, not a breaking change.

## Out of scope
- Pagination beyond the 20-issue cap (dashboard notes the cap when more issues exist, but doesn't page through them).
- Filtering/sorting the dashboard by stage or label.

## Verification
- unit: `bash tests/otta-status.test.sh` — added cases 6 (dashboard mode renders one row per issue with per-stage glyphs) and 7 (empty issue list doesn't crash); cases 1-5 (single-issue mode) re-verified unchanged and still passing.
- e2e: n/a — command orchestration (`gh issue list` + per-issue stage resolution) is prose-driven by the agent, not scriptable; renderer logic is the testable unit and is covered above.
- preview: n/a — no preview env for this CLI-plugin command.
```

## Acceptance

- [x] AC1: dashboard mode on empty `$1` — `commands/status.md` "Dashboard mode (no `$1`)" section added, no issue number required.
- [x] AC2: enumerate open issues via `gh issue list --limit 20`, reusing single-issue stage-resolution steps 1-6 per issue (documented, not duplicated).
- [x] AC3: one compact row per issue (issue #, title, 5 glyphs) — `render_dashboard()` in `scripts/otta-status.sh`.
- [x] AC4: `scripts/otta-status.sh` detects an `"issues"` array and dispatches to `render_dashboard`; unit-tested in `tests/otta-status.test.sh` cases 6-7.
- [x] AC5: single-issue mode untouched — `tests/otta-status.test.sh` cases 1-5 pass unmodified.

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#84

Fixes #84
