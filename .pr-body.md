## Summary

- Scheduled/cloud routines (`commands/schedule.md`) and headless `/otta:build -p` runs (`commands/build.md`) now recommend `CLAUDE_CODE_DISABLE_ADVISOR_TOOL=1` in their Claude Code launch environment, so no subagent burns a nondeterministic, uncached transcript re-read consulting the advisor with nobody watching.
- Interactive `/otta:dev` is untouched — the advisor stays available where a developer is present to benefit from it.

```acceptance
GIVEN a scheduled Claude Code routine or a headless `/otta:build -p` run
WHEN  the run launches its Claude Code environment
THEN  CLAUDE_CODE_DISABLE_ADVISOR_TOOL=1 is set, while interactive /otta:dev leaves the advisor enabled
```

- [x] `commands/schedule.md` Claude Code guidance sets `CLAUDE_CODE_DISABLE_ADVISOR_TOOL=1` in the scheduled/cloud-routine launch environment, with one line of rationale (deterministic cost). Verified by a test asserting the env var appears in schedule.md.
- [x] `commands/build.md` documents the same recommendation for headless/unattended invocations (`-p` runs), explicitly NOT applying it to interactive `/otta:dev`. Verified by grep test.
- [x] Guidance is availability-gated/harness-scoped: Claude Code only; Codex/Gemini sections untouched (env var is a no-op elsewhere but must not be presented as cross-harness).
- [x] Interactive `/otta:dev` behavior unchanged (no advisor mention added there beyond, at most, a debugging note).

## Out of scope
- Changing the user's global `advisorModel` setting.
- Any hooks.json change.

## Verification
- unit: `tests/advisor-autonomous-runs.test.sh` (4 checks, all passing)
- e2e: n/a — doc/guidance-only change, no runtime behavior to drive
- preview: n/a — no preview env

## Acceptance Evidence

- [x] AC1: schedule.md sets `CLAUDE_CODE_DISABLE_ADVISOR_TOOL=1` with rationale — tests/advisor-autonomous-runs.test.sh check 1
- [x] AC2: build.md documents the same for headless `-p` runs, explicitly excluding `/otta:dev` — tests/advisor-autonomous-runs.test.sh checks 2-3
- [x] AC3: guidance placed only inside Claude Code-scoped sections of schedule.md/build.md; Codex/Gemini sections untouched (verified by diff review)
- [x] AC4: dev.md unchanged apart from no advisor mention — tests/advisor-autonomous-runs.test.sh check 4

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#160

Fixes #160
