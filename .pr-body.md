## Summary

- Moves the blocking shellcheck gate from `--severity=error` to `--severity=warning`, and fixes the 5 warning findings that were blocking that move.
- #177 gated at `error` because the warning volume was unknown. Measured, it was 5 — small enough that gating them is cheap and worth it.
- `info` (12) and `style` (14) stay report-only. Those are the real quoting-cleanup bulk; gating them would be churn, not safety.

```acceptance
GIVEN a shell script in scripts/ or hooks/ with a warning-severity shellcheck finding
WHEN  CI runs on the pull request
THEN  the lint job fails, while info and style findings still only print
```

- [x] [data-layer] The blocking CI run uses `--severity=warning`.
- [x] [data-layer] All 5 existing warning-severity findings are fixed.
- [x] [data-layer] `info`/`style` findings still print without failing the build.
- [x] [data-layer] `tests/shellcheck-gate.test.sh` asserts the gate is at warning severity and the tree is clean there.
- [x] [data-layer] Full `tests/*.test.sh` suite passes.

## The 5 fixes

| file | code | fix | runtime effect |
|---|---|---|---|
| `otta-repair-loop.sh:31` | SC2209 | `labels=test` → `labels="test"` | none — same literal string, quoted so shellcheck stops reading it as a missing command substitution |
| `github-workflow-deploy.sh:96` | SC2034 | drop unused `line` from the `local` declaration | none — it was declared and never referenced |
| `subagent-gate-guard.test.sh:22,42,60` | SC2164 | `cd "$d"` → `cd "$d" \|\| exit 1` | none reachable — the target is a just-created `mktemp -d` |

All five are no-op hardening. Nothing observable changes, which is why there is no new behavioural regression test: **the tightened gate itself is the regression protection.** `tests/shellcheck-gate.test.sh` asserts the gate is wired, is not neutered with `|| true`, and that the tree is clean at the gated severity — so any reintroduced warning fails CI.

## Verification

- unit: `tests/shellcheck-gate.test.sh` retargeted to warning severity and proven RED before the workflow change — *"ci.yml must run shellcheck with --severity=warning as a blocking gate"* — then green.
- unit: full suite — 77/77 pass.
- lint: `shellcheck --severity=warning --format=gcc scripts/*.sh hooks/*.sh` — **no output, clean**.
- direct: `bash hooks/subagent-gate-guard.test.sh </dev/null` → **6 passed, 0 failed** after the SC2164 edits.
- e2e: n/a — CI configuration.
- preview: n/a — no preview environment.

## Acceptance Evidence

- [x] AC1: the gate step is now `--severity=warning`; case 1 of the test asserts it and case 2 asserts it carries no `|| true`.
- [x] AC2: all 5 fixed — shellcheck at warning severity produces no output.
- [x] AC3: the report step is unchanged at `--severity=style` with `|| true`, so info/style still print without blocking. Case 3 now asserts the report is `(style|info)`, so a future edit cannot satisfy it by pointing the *report* at warning and quietly dropping the gate.
- [x] AC4: cases 1, 2 and 6 cover wiring, non-suppression, and clean-tree at warning.
- [x] AC5: 77/77.

## Note for the reviewer — an orphan test

`hooks/subagent-gate-guard.test.sh` is **never run by CI**. The workflow loops `tests/*.test.sh`, and this file lives in `hooks/`. It passes (6/6, verified above), but nothing enforces that.

Pre-existing and out of scope here, but worth fixing separately — either move it to `tests/` or widen the glob. Flagging rather than folding it in, since widening the glob could surface unrelated failures that belong in their own PR.

It also blocks on `stdin` when run without redirection, because the hook under test does `input="$(cat)"`. Harmless in CI, surprising by hand.

idea_ref: issue:#182

Fixes #182
