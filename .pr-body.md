Zero-paste Pulse wiring: `/otta:setup` now auto-writes `.otta/pulse.env` (chmod 600, gitignored) so the plugin streams verdicts to Pulse without any manual shell-profile step.

idea_ref: otta-zero-paste-pulse

Fixes #5

```acceptance
[x] No ~/.zshrc / shell-profile step for Pulse (dead manual step removed)
[x] ledger-append.sh keeps writing local jsonl (back-compat) + streams best-effort only when both vars set
[x] failing/timing-out /ledger POST never changes exit code (shell test proves it)
[x] pulse-install.sh auto-writes gitignored .otta/pulse.env (chmod 600), idempotent, non-fatal on /connect failure
[x] setup.md onboarding rewritten — zero token paste
```

## Test plan

All 7 tests in `tests/*.test.sh` pass green:

- `tests/ledger-stream.test.sh` — 5 cases: no-vars back-compat, bogus-URL non-fatal, `OTTA_NO_CAPTURE` suppression, `.env` file sourcing, env-var precedence over file
- `tests/ledger-append.test.sh` — local jsonl write still works
- `tests/detect-delivery-context.test.sh`, `tests/fix-command.test.sh`, `tests/otta-worktree.test.sh`, `tests/qa-dry-run.test.sh`, `tests/verdict-capture.test.sh` — existing suite unchanged
