## Summary

- Verified against gemini-cli source (v0.52.0, `packages/core/src/telemetry/sdk.ts` + `config.ts`) that Gemini CLI exports OTLP **directly** — no collector sidecar is required for `telemetry.target: "local"` (the default). The previous script's collector-sidecar approach was based on an incorrect premise.
- Auth headers ARE supported: Gemini's OTLP exporters are constructed with only a `url` (no explicit `headers`), so `@opentelemetry/otlp-exporter-base` falls back to the standard `OTEL_EXPORTER_OTLP_HEADERS` env var.
- Discovered and fixed a second gap: Gemini's folder-trust feature (default ON) silently drops workspace-scoped `.gemini/settings.json` — including our telemetry block — unless the folder is trusted. Script now also sets `GEMINI_CLI_TRUST_WORKSPACE=true` (the documented CI/automation bypass), or the config would appear written but never take effect.
- Rewrote `otta-gemini-setup.sh` to write `.gemini/settings.json` (`telemetry.enabled/target/otlpEndpoint/otlpProtocol`) + `.otta/gemini.env` (`OTEL_EXPORTER_OTLP_HEADERS`, `OTEL_RESOURCE_ATTRIBUTES`, `GEMINI_CLI_TRUST_WORKSPACE`) instead of a docker-dependent OTel Collector sidecar; added `--derive` mode matching `otta-codex-setup.sh`.

```acceptance
GIVEN a repo with Gemini CLI detected and a Pulse repo token
WHEN  `otta-gemini-setup.sh <owner/repo> <token>` runs
THEN  `.gemini/settings.json` has telemetry.otlpEndpoint pointed at Pulse and `.otta/gemini.env` carries the OTLP auth header + workspace-trust bypass — no collector/docker step required
```

- [x] AC1: Gemini CLI OTEL mechanism documented (env vars vs settings.json vs collector) — direct OTLP/HTTP export via `.gemini/settings.json` `telemetry` block (target=local); auth injected via standard `OTEL_EXPORTER_OTLP_HEADERS` env var; workspace-trust gate documented (all source-verified, not just docs)
- [ ] AC2: Verified Gemini emits to a capture server (session, tokens, model) — **blocked on user re-auth**: local Gemini CLI account (`oauth-personal`, free tier) now hits `IneligibleTierError: This client is no longer supported for Gemini Code Assist for individuals` — a harder block than the expired-token state described in the issue; this is a Google-side deprecation of the free-tier OAuth client, not a token refresh. Cannot be resolved without the user switching auth method (e.g. API key) or migrating to Antigravity.
- [x] AC3: `otta-gemini-setup.sh` correctly configures whatever mechanism Gemini uses — rewritten to direct OTLP export (settings.json) + env-var auth header + workspace-trust bypass (.otta/gemini.env), matches --derive pattern from otta-codex-setup.sh
- [ ] AC4: `/compare` shows a Gemini session row after a real gemini run — **blocked on AC2's auth issue, AND likely needs Pulse-side work even once auth is fixed.** Gemini emits different attribute keys than Codex/Claude Code: event `gemini_cli.api_response` with `gen_ai.usage.input_tokens`/`gen_ai.usage.output_tokens` (GenAI semantic-convention style), vs. Codex's `conversation.id`/`input_token_count` which needed its own normalizer (#50). `session.id` may line up with the existing `normalizeCC` path, but token aggregation almost certainly needs a Gemini-aware normalizer on Pulse before `/compare` would show real numbers — restoring auth alone will not be sufficient. Filing this as a follow-up for the Pulse side; out of scope for this plugin-repo issue.

## Out of scope
- Fixing/working around the Gemini CLI free-tier OAuth deprecation (user auth decision, not a plugin bug)
- Pulse-side normalizer for Gemini's `gen_ai.*` attribute keys (needed for AC4 even after auth is restored — separate repo/issue)
- Wiring `otta-gemini-setup.sh` into `commands/setup.md` Part B execution (it's currently only referenced in the AskUserQuestion prompt, not actually invoked — pre-existing gap, separate from this issue's scope)

## Verification
- unit: `tests/otta-gemini-setup.test.sh` (15 tests, all passing) — settings.json telemetry fields, env-file auth header + resource attrs + workspace-trust round-trip, idempotency, gitignore, --derive hosted/self-hosted modes, no-collector-file assertion, no-docker-in-output assertion
- e2e: n/a — blocked on Gemini CLI free-tier OAuth deprecation (see AC2)
- preview: n/a — no preview env

## Acceptance Evidence

- [x] AC1: documented in script header (scripts/otta-gemini-setup.sh) with source citations
- [x] AC3: tests/otta-gemini-setup.test.sh — 15/15 passing
- [ ] AC2, AC4: blocked-on-user-reauth (AC4 also needs Pulse-side normalizer work) — see above

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#51

[test-impractical: AC2/AC4 require a live Gemini CLI session; local account hit a Google-side free-tier OAuth deprecation (IneligibleTierError), not the expired-token state the issue anticipated — needs a user auth-method decision to unblock, and AC4 additionally needs a Pulse-side normalizer for Gemini's gen_ai.* attribute keys]

Fixes #51
