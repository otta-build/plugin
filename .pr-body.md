## Summary

- pre-push-guard.sh now resolves the repo actually targeted by a `git push` command (`-C <path>`, cumulative `-C`, `cd <path> &&` compound forms, and subshell-wrapped `(cd <path> && git push)`) and gates that repo's `.pr-body.md`, instead of always gating the session cwd's repo.
- Ambiguous or unresolvable *targets* (e.g. multiple pushes to different repos in one compound command) fail closed to the session repo. This covers resolution failures only — a known detection gap (line-continuation `git \`+newline+`push` isn't recognized as a push at all) still fails open and is tracked as #161.
- Push detection is metachar-safe: `push` followed by `)`, `&`, `|`, `>`, etc. (e.g. `(git push)`, `git push&`) is still detected, closing a fail-open gap where those forms silently skipped the gate entirely.

```acceptance
GIVEN a session cwd inside a gated repo with a stale/failing .pr-body.md
WHEN  the agent runs `git -C <other-repo> push` or `cd <other-repo> && git push`
THEN  the gate does not run and the push is not blocked by the session repo's .pr-body.md
```

- [x] pre-push-guard.sh resolves the repository actually targeted by the push command (honor `git -C <path>`, `-C` before subcommand, and cwd-relative pushes) and runs the gate only when that repo is the Otta-gated repo.
- [x] A push targeting a different repo from within a gated-repo session cwd is NOT gated (regression test with a temp secondary repo).
- [x] A push targeting the gated repo still gates exactly as today (existing tests pass).
- [x] Stale-post-merge `.pr-body.md` (Fixes #N where N closed) in the session repo no longer blocks unrelated pushes (covered by the scoping fix; test asserts).

## Out of scope
- Full shell parsing (pipes, subshells, quoting edge cases) — compound-command splitting is conservative (`&&`, `||`, `;`). Fail-closed only covers *resolution* failures (an unresolvable or ambiguous `-C`/`cd` target); it does not cover *detection* failures. A known detection gap — backslash line-continuation (`git \` + newline + `push`) is not recognized as a push at all, so it fails open — is tracked separately in #161.

## Verification
- unit: `tests/pre-push-guard-target-repo.test.sh` (new, 7 checks) + `tests/pre-push-guard-nested.test.sh` (existing, still green) — `bash tests/pre-push-guard-target-repo.test.sh`
- e2e: n/a — hook-level bash script, no UI/user flow
- preview: n/a — no preview env for a Bash PreToolUse hook

## Acceptance Evidence

- [x] AC1: target-repo resolution via `-C`/`cd`/subshell — `tests/pre-push-guard-target-repo.test.sh` checks 1, 2, 4, 7
- [x] AC2: different-repo push not gated — `tests/pre-push-guard-target-repo.test.sh` checks 1, 2, 7 (exit 0, no "otta-gate" output)
- [x] AC3: same-repo push still gated, including metachar-suffixed forms that previously fail-opened — `tests/pre-push-guard-target-repo.test.sh` checks 3, 5, 6 + existing `tests/pre-push-guard-nested.test.sh` both pass
- [x] AC4: stale .pr-body.md no longer blocks unrelated pushes — covered by checks 1, 2, 7 (failing session .pr-body.md present but gate never runs)

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#159

Fixes #159
