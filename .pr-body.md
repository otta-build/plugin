## Summary

- Release v0.16.1: bump `.claude-plugin/plugin.json` 0.16.0 → 0.16.1 and add the CHANGELOG entry for the onboarding metrics-exporter fix (plugin#43, merged in #44)
- Cuts the release so customers pulling the plugin get the fixed `otta-telemetry-setup.sh` (writes the OTLP metrics exporter CC actually emits to)

```acceptance
GIVEN plugin#43 (metrics exporter in otta-telemetry-setup.sh) is merged to main
WHEN  the version is bumped to 0.16.1 and tagged
THEN  the marketplace release ships the fixed onboarding script to customers

- [x] AC1: plugin.json version = 0.16.1
- [x] AC2: CHANGELOG has a 0.16.1 entry describing the metrics-exporter fix

## Out of scope
- The fix itself (shipped in #44 / plugin#43)

## Verification
- unit: n/a [test-impractical: release version bump + CHANGELOG only — no behavior change; the fix it releases is already tested in tests/otta-telemetry-setup.test.sh via #44]
- e2e: n/a — covered by pulse#50 + plugin#43 e2e
- preview: n/a
```

## Acceptance

idea_ref: issue:#43

Fixes #43
