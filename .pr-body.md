## Summary

- `scripts/otta-telemetry-setup.sh` now prints restart reminder after writing telemetry config
- CC reads `settings.local.json` at startup only; config written mid-session has no effect until restart

```acceptance
- [ ] AC1: script output includes "Restart Claude Code" after successful write
- [ ] AC2: no other behavior changed

idea_ref: issue:#41
Fixes #41
```

## Verification

```bash
bash scripts/otta-telemetry-setup.sh owner/repo <secret> | grep 'Restart Claude Code'
```

- [x] AC1: added `echo "  ↻ Restart Claude Code..."` after success line — single line addition
- [x] AC2: all other logic unchanged (token derivation, file write, gitignore)

idea_ref: issue:#41
Fixes #41
