## Summary

- Emit Codex-schema-compatible inline OTLP/HTTP objects for logs and metrics.
- Migrate the invalid v1.1.2 selector/subtable form without clobbering unrelated OTEL config.
- Regression-test schema shape, idempotence, escaping, permissions, and legacy migration.

```acceptance
GIVEN a fresh config or one containing Otta's v1.1.2 OTEL table form
WHEN the Codex telemetry setup is run or rerun
THEN Codex loads schema-valid inline exporters and preserves unrelated config
```

- [ ] AC1 [test]: generated Codex config parses with the current Codex CLI strict config loader.
- [ ] AC2 [test]: log and metrics exporters use the schema-supported inline `otlp-http` object with endpoint, json protocol, x-pulse-token, and x-pulse-repo.
- [ ] AC3 [test]: rerun replaces prior Otta inline or legacy table forms without clobbering unrelated `[otel]` keys or literal quoted tables.
- [ ] AC4 [test]: hostile values remain safely TOML-escaped and token files stay mode 0600.
- [ ] AC5 [e2e]: a fresh child Codex process starts and emits a repo-attributed event accepted by Pulse.

<!-- AC layer key:
     [data-layer] — schema + mutations + unit tests, no UI required
     [ui-layer]   — requires working page/component visible in the app
     [e2e]        — requires full user flow: link → action → observable result
     Evidence required: [data-layer] → unit test OK; [ui-layer]/[e2e] → preview URL or e2e tool. -->
## Out of scope
- Changes to Pulse ingestion or Claude Code telemetry, which already accept production traffic.

## Verification
- unit: `bash tests/otta-codex-setup.test.sh`
- suite: all 40 `tests/*.test.sh` files plus `node --test tests/repair-policy.test.mjs`
- e2e: fresh child Codex process plus Pulse comparison after release
- preview: n/a — CLI plugin release

## Acceptance Evidence

- [x] AC1: generated config parses as TOML and uses Codex's schema shape — `otta-codex-setup.test.sh` schema assertion.
- [x] AC2: logs and metrics contain endpoint, JSON protocol, token, and repo — parsed-object assertions.
- [x] AC3: reruns migrate legacy tables and preserve unrelated/literal tables — merge and literal-table regressions.
- [x] AC4: hostile values round-trip safely and files remain mode 0600 — hostile-value and permission regressions.
- [ ] AC5: fresh Codex process emits repo-attributed production telemetry — to be recorded after release installation.

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#133

<!-- If no test is practical, add: [test-impractical: <reason>] -->

Fixes #133
