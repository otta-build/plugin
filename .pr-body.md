## Summary

- Codex adapter sections of `start.md`/`dev.md` now set Codex's persisted `/goal` from the issue's ACs alongside `update_plan`, with an explicit `update_plan`-only fallback.
- Documented the Codex `SubagentStart`/`SubagentStop` + session-ID-inheritance Pulse join key, the seconds-based OTEL histogram unit change, and the `hide_spawn_agent_metadata` debugging default.
- Added headless `codex exec resume --output-schema` guidance to `build.md`, and `explicit-request-only`/`writes` delegation+approval-mode recommendations to `setup.md`.
- Added a regression-guard test for the known upstream breaking changes (removed custom-prompt files, renamed agent tools, hooks.json top-level keys).

```acceptance
GIVEN Codex CLI shipped a persisted goal system, subagent lifecycle hooks, headless resume with structured output, and delegation/approval controls since v1.6.0's Codex audit
WHEN  a developer runs /otta:start, /otta:dev, /otta:build, or /otta:setup with Codex as the active harness
THEN  the Codex adapter sections instruct using the new primitives where available, fall back gracefully on older Codex versions, and Claude Code behavior is unchanged
```

- [x] (g1) Codex adapter sections of `commands/start.md` and `commands/dev.md` instruct using Codex's persisted goal system (`/goal`) where available — goal set from the issue's acceptance criteria at pipeline start — alongside the existing `update_plan` protocol, with explicit fallback to `update_plan`-only on Codex versions lacking goals. Verified by a test asserting goal instruction + fallback text in both files' Codex sections.
- [x] (g2) Per-stage telemetry correlation documented/wired for Codex: `scripts/otta-codex-setup.sh` and/or `commands/setup.md` Codex telemetry step document `SubagentStart`/`SubagentStop` hook events, subagent identity in hook inputs, and parent session-ID inheritance (Codex v0.131–0.134) so Pulse can join pipeline stages to sessions. Verified by a test asserting SubagentStart + session-ID inheritance appear in the Codex telemetry surface.
- [x] (g3) Headless Codex guidance (`commands/build.md` and/or `commands/schedule.md` Codex sections) instructs `codex exec resume --output-schema` for structured stage verdicts when resuming headless runs, gated on availability. Verified by a test asserting the flag appears in a Codex adapter section.
- [x] (g4) Setup/AGENTS.md Codex guidance recommends explicit multi-agent delegation control (`explicit-request-only`) for pipeline runs so stages don't self-delegate, and documents the `writes` approval mode as the recommended CI/headless setting (Codex v0.142/v0.144). Verified by a test asserting both recommendations exist in the Codex setup surface.
- [x] (g5) Regression guards for upstream breaking changes: a test asserts (i) no reliance on removed file-based custom prompts (no `~/.codex/prompts` references; `.codex-plugin/plugin.json` retains the `skills` pointer), (ii) no use of removed/renamed agent tool names (`close_agent`, `assign_task`) anywhere in commands/skills/agents, (iii) `hooks/hooks.json` top-level keys restricted to the supported set.
- [x] (g6) OTEL unit-change note: Codex ≥0.141 emits seconds-based duration histograms — documented in the Codex telemetry surface so Pulse dashboards are checked against it; `hide_spawn_agent_metadata` default (v0.137) noted in the Codex adapter debugging guidance. Verified by grep test.
- [x] All additions availability-gated: older Codex versions and non-Codex harnesses keep current behavior; Claude Code adapter sections untouched by this change.
- [x] Ships as a single release: PR merges to main with a `feat:` title so auto-release.yml bumps v1.6.0 → v1.7.0 and generates the CHANGELOG entry (release automation authoritative).

## Out of scope
- Codex plugin marketplace / npm distribution (separate distribution issue, not plugin code).
- Hooks manifest harness-split (#155).
- Rewriting `otta-deploy-verify.sh` or any Claude Code surface.

## Verification
- unit: `tests/codex-goal-protocol.test.sh`, `tests/codex-telemetry-correlation.test.sh`, `tests/codex-headless-resume.test.sh`, `tests/codex-delegation-approval.test.sh`, `tests/codex-breaking-guards.test.sh` (all new, all passing); full suite `for t in tests/*.test.sh; do bash "$t"; done` — 62/62 passing including `tests/codex-plugin-parity.test.sh`.
- e2e: n/a — documentation/config-surface change with no runtime UI/flow to drive; every AC is grep/jq-verified against the actual adapter text and manifest files.
- preview: n/a — no preview env; this is a Claude Code plugin, not a deployed app.

## Acceptance Evidence

<!-- After merge, echo each AC with evidence:
- [x] AC1: <restated> — <evidence: test name / preview obs>
-->

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: idea:idea-2026-07-14-002

<!-- If no test is practical, add: [test-impractical: <reason>] -->

Fixes #157
