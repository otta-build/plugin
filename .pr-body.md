## Summary

- Added `displayName`, `icon` (🦦), `examplePrompt`, and (marketplace-only) `category: "Productivity"`/`homepage` to `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`, mirrored `icon`/`examplePrompt` into `.codex-plugin/plugin.json`.
- Trimmed the two SKILL.md descriptions that carried redundant trigger boilerplate already covered in-body (`learn-from-pulse`, `shipping-loop`); the other 10 skills were already crisp one-liners.
- Extended `tests/marketplace-version-sync.test.sh` to assert the new fields are present and synced, and that `claude plugin validate` reports no load-breaking errors.

```acceptance
GIVEN the otta plugin/marketplace manifests without listing metadata
WHEN category, homepage, displayName, icon, and examplePrompt are added and `claude plugin validate .` is run
THEN the new fields are present/synced across manifests and Claude Code loads the plugin without error (icon/examplePrompt are unknown-but-gracefully-ignored fields per `claude plugin validate --strict`)
```

- [x] AC1: plugin.json/marketplace.json gain: category ("Productivity"), homepage (https://otta.build), author display name, icon asset, examplePrompt ("Run /otta:dev on issue #42 — build, review, verify, ship")
- [x] AC2: every skill's SKILL.md frontmatter description reads as a crisp one-liner (audit all; trim trigger-condition boilerplate into a separate `when:`-style paragraph in the body where needed — keep Claude Code trigger behavior intact)
- [x] AC3: marketplace-version-sync test extended to assert new fields present
- [x] AC4: fields degrade gracefully in Claude Code's own /plugin UI (no breakage)

## Out of scope
- **NEEDS_CONTEXT (icon format):** used a placeholder emoji (🦦) for `icon` since no branding/image asset exists in the repo and cockpit's Plugins page (otta-build/cockpit#2, unbuilt) doesn't yet specify whether it renders `icon` as text/emoji or requires an image path. Confirmed against cockpit#2's design section that it reads `.claude-plugin/plugin.json`/`marketplace.json` top-level `category`, `website`(=`homepage`), `icon`, `examplePrompt`, `developer`(=`author.name`) — exactly the fields/locations this PR adds — so the field *names* are verified correct; only the icon asset *format* is open. Owner should confirm whether cockpit needs an image asset before that page is built.
- `.codex-plugin/plugin.json`'s pre-existing `interface.category` ("Developer Tools") was left untouched — that block belongs to a different (Codex) harness surface, not cockpit's data source per cockpit#2. Only `icon`/`examplePrompt` were mirrored there for forward Codex-side parity (not required by any open AC).

## Verification
- unit: `bash tests/marketplace-version-sync.test.sh` (extended); full suite `for f in tests/*.test.sh; do bash "$f"; done` — 66/66 pass
- e2e: `claude plugin validate .` (non-strict, authoritative load check) passes; `claude plugin validate . --strict` shows exactly the 2 expected unknown-field warnings (icon, examplePrompt) × 2 manifest locations, confirming graceful degradation
- preview: n/a — no preview env for plugin marketplace metadata

## Acceptance Evidence

<!-- After merge, echo each AC with evidence:
- [x] AC1: <restated> — <evidence: test name / preview obs>
-->

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#117

<!-- If no test is practical, add: [test-impractical: <reason>] -->

Fixes #117
