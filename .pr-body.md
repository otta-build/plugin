## Summary

- Adds a native Codex plugin manifest, ten discoverable `$otta-*` skills, portable hooks, and Codex-native orchestration fallbacks while preserving Claude Code commands.
- Adds executor/session/issue/PR/branch attribution to the local ledger without breaking legacy callers.
- Makes Claude and Codex telemetry setup explicit, secret-safe, idempotent, and repo-attributed for Pulse ingestion.

```acceptance
GIVEN Otta is installed in Claude Code or Codex
WHEN  a developer invokes the matching Otta workflow or enables telemetry
THEN  the workflow runs with native runtime primitives and emits repo-attributed evidence without exposing credentials
```

- [x] AC1 [test]: Hook commands resolve from Codex-injected PLUGIN_ROOT or Claude's CLAUDE_PLUGIN_ROOT, while ambient CODEX_PLUGIN_ROOT is ignored.
- [x] AC2 [test]: The package contains a valid .codex-plugin/plugin.json with interface metadata and explicit skills/hooks paths, while the Claude manifest remains valid.
- [x] AC3 [test]: Codex exposes setup, start, dev, build, fix, ship, status, schedule, remember, and pulse-doctor as native skills, with native orchestration/input fallbacks and truthful scheduling limits.
- [x] AC4 [test]: Ledger records include executor, harness, session_id, issue, pr, and branch when provided, without breaking older callers.
- [x] AC5 [test]: Codex and Claude telemetry setup produce authenticated repo/harness attribution inputs without committing or printing tokens.
- [x] AC6 [review]: README documents the real Claude and Codex install/invocation UX without claiming a nonexistent runtime panel.
- [x] AC7 [test]: Focused tests, full plugin shell suite, and package metadata validation pass.

## Out of scope
- A new Otta application or cockpit UI.
- A Codex-native cloud scheduler; the skill provides a truthful external-automation handoff.
- Pulse server-side Codex materialization, tracked separately in otta-build/pulse#117.

## Verification
- unit: `for t in tests/*.test.sh; do bash "$t"; done` (with temporary PyYAML test dependency on PATH) — all 40 shell test files passed
- e2e: `bash tests/codex-plugin-parity.test.sh`; `bash tests/otta-codex-setup.test.sh`; `bash tests/otta-telemetry-setup.test.sh`; `bash tests/ledger-stream.test.sh`
- preview: n/a — developer-tool plugin; install/runtime smoke follows the released package

## Acceptance Evidence

- [x] AC1-AC3: `tests/codex-plugin-parity.test.sh`, `tests/setup-wizard.test.sh`, `tests/pipeline-checklist.test.sh`
- [x] AC4: `tests/ledger-append.test.sh`, `tests/ledger-stream.test.sh`
- [x] AC5: `tests/otta-codex-setup.test.sh`, `tests/otta-telemetry-setup.test.sh`
- [x] AC6: final spec and quality review
- [x] AC7: complete per-file `tests/*.test.sh` loop

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#131

<!-- If no test is practical, add: [test-impractical: <reason>] -->

Fixes #131
