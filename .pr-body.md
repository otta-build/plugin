## Summary

<!-- 1-3 bullets: what this PR does (issue: feat(setup): wire CC OTEL logs/traces telemetry → Pulse (emit side of #38)) -->

```acceptance
GIVEN <!-- starting state / context -->
WHEN  <!-- the action or trigger -->
THEN  <!-- observable, verifiable outcome -->

- [ ] AC1: `/otta:setup` offers (opt-in prompt) to enable Pulse telemetry; on consent, writes the **logs** env into `.claude/settings.local.json` `env` block — **merge into existing, never clobber**; idempotent on re-run
- [ ] AC2: logs env set = `CLAUDE_CODE_ENABLE_TELEMETRY=1`, `OTEL_LOGS_EXPORTER=otlp`, `OTEL_EXPORTER_OTLP_LOGS_PROTOCOL=http/json`, `OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=${pulse}/v1/logs`, `OTEL_EXPORTER_OTLP_HEADERS=x-pulse-token=<repo-token>`, `OTEL_RESOURCE_ATTRIBUTES=repo=<owner/repo>` — endpoint from `OTTA_PULSE_URL`, repo+token from the existing pulse wiring
- [ ] AC3: traces are a SEPARATE opt-in (flagged beta, default NO) → adds `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1`, `OTEL_TRACES_EXPORTER=otlp`, `OTEL_EXPORTER_OTLP_TRACES_PROTOCOL=http/json`, `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${pulse}/v1/traces`
- [ ] AC4: any token-bearing value lands ONLY in the gitignored file; `.gitignore` covers `.claude/settings.local.json`; committed `settings.json` is never written with the token (verify)
- [ ] AC5: a writer script (e.g. `scripts/otta-telemetry-setup.sh`) does the JSON merge so it is unit-testable without the interactive command
- [ ] AC6: docs — `setup.md` step + README document the env block (incl. the manual block for non-setup users) and the logs-default / traces-beta split
- [ ] AC7: tests — `.test.sh`: logs-only by default; traces added on opt-in; valid JSON merge into a pre-existing `env`; token never written to committed `settings.json`; idempotent re-run

## Out of scope
- <!-- things explicitly NOT built in this issue -->

## Verification
- unit: <!-- test file / command, or "n/a — reason" -->
- e2e: <!-- e2e spec / flow, or "n/a — reason" -->
- preview: <!-- deployed-env observation, or "n/a — no preview env" -->
```

## Acceptance

<!-- After merge, echo each AC with evidence:
- [x] AC1: <restated> — <evidence: test name / preview obs>
-->

<!-- idea origin (where this work came from) — Pulse reads this for the idea→version chain.
     Replace with the real origin if different: intercom:..., sentry:..., linear:... -->
idea_ref: issue:#22

<!-- If no test is practical, add: [test-impractical: <reason>] -->

Fixes #22
